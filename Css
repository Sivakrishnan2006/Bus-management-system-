body {
    font-family: Arial, sans-serif;
    text-align: center;
    background: #f2f2f2;
}
input, button {
    padding: 10px;
    margin: 5px;
}
button {
    background-color: green;
    color: white;
    border: none;
    cursor: pointer;
}
button:hover {
    background-color: darkgreen;
}
